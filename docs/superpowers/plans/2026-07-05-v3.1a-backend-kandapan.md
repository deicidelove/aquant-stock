# v3.1A — 看大盘后端（研判/指数走势/量能）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为驾驶舱"看大盘"提供 3 个只读端点：今日研判（regime）、指数走势+均线（收盘线+MA20/60）、两市量能（成交额趋势）。

**Architecture:** `aquant/macro.py` 新增 3 个纯领域函数（只读 DuckDB，复用 `market.regime`、`index_daily`、`daily_bar`）；FastAPI 挂到既有 macro 路由。

**Tech Stack:** Python 3.11 · FastAPI · DuckDB(`aquant.data.store`) · pandas · pytest + httpx TestClient。

## Global Constraints

- Python 3.11；新增逻辑放 `aquant/macro.py`（已存在，含 sentiment/market_fund_trend/indices/sector_fund_rank/abnormal_fund）。
- DB 路径由 `AQUANT_DATA_DIR` 驱动；测试在 import 任何 aquant 前设置（沿用 `tests/conftest.py`）。
- API 请求处理内**禁止**第三方网络；只读 `store`。
- 端点路径 `/api/cockpit/*`，pydantic 响应模型；挂在既有 `server/routers/macro.py`。
- 提交信息结尾加：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 在分支 `v3.1a-backend` 上开发；每 Task 末尾提交。
- 复用契约（已核对）：
  - `aquant.market.regime() -> dict`：`{state, score, suggested_position, note, breadth, index}`（数据不足返回 `{}`）。
  - `index_daily` 表：列 `[code, date, close]`，主键 `[code, date]`；含 `sh000300`。
  - `daily_bar` 表：含 `date, amount`。
  - `aquant.data.store.query/has_table`。

---

### Task 1: 领域函数 regime / index_series / amount_trend

**Files:**
- Modify: `aquant/macro.py`
- Test: `tests/test_macro_kandapan.py`

**Interfaces:**
- Consumes: `aquant.market.regime`；`store`（读 `index_daily`/`daily_bar`）
- Produces（`aquant/macro.py` 追加）：
  - `regime() -> dict`：返回 `market.regime()`（透传；数据不足 `{}`）
  - `index_series(code: str = "sh000300", n: int = 120) -> dict`：`{code, points:[{date, close, ma20, ma60}]}`，读 `index_daily` 该 code 全序列算 MA 后取尾 n；窗口不足的 ma 为 None；无表/无数据返回 `{code, points: []}`
  - `amount_trend(days: int = 20) -> dict`：`{series:[{date, amount}]}`，`daily_bar` 按 date `sum(amount)` 升序取尾 days，amount 为亿元（/1e8，2 位）；无表返回 `{series: []}`

- [ ] **Step 1: 写失败测试**

`tests/test_macro_kandapan.py`：
```python
import pandas as pd


def test_regime_passthrough(seed_db, monkeypatch):
    from aquant import macro, market
    monkeypatch.setattr(market, "regime", lambda: {"state": "防守", "score": 1, "suggested_position": "1~3成", "note": "x"})
    assert macro.regime()["state"] == "防守"


def test_index_series(seed_db):
    from aquant import macro
    # 注入指数收盘序列（70 日，够算 MA20，不够 MA60）
    dates = pd.bdate_range("2026-01-01", periods=70).strftime("%Y-%m-%d").tolist()
    seed_db.save("index_daily", pd.DataFrame([
        {"code": "sh000300", "date": d, "close": 3900 + i} for i, d in enumerate(dates)]))
    out = macro.index_series("sh000300", n=30)
    assert out["code"] == "sh000300"
    assert len(out["points"]) == 30
    p = out["points"][-1]
    assert set(["date", "close", "ma20", "ma60"]).issubset(p)
    assert p["ma20"] is not None            # 70>=20
    assert p["ma60"] is not None            # 70>=60


def test_amount_trend(seed_db):
    from aquant import macro
    # seed_db 的 daily_bar 有 amount（2 只×80 日）
    out = macro.amount_trend(days=5)
    assert isinstance(out["series"], list) and len(out["series"]) == 5
    assert set(["date", "amount"]).issubset(out["series"][0])
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/test_macro_kandapan.py -v`
Expected: FAIL（`AttributeError: ...'index_series'` 等）

- [ ] **Step 3: 实现**

`aquant/macro.py` 追加（顶部已 `import pandas as pd`、`from . import market`、`from .data import store`）：
```python
def regime() -> dict:
    return market.regime()


def index_series(code: str = "sh000300", n: int = 120) -> dict:
    if not store.has_table("index_daily"):
        return {"code": code, "points": []}
    df = store.query("SELECT date, close FROM index_daily WHERE code = ? ORDER BY date", [code])
    if df.empty:
        return {"code": code, "points": []}
    df["ma20"] = df["close"].rolling(20).mean()
    df["ma60"] = df["close"].rolling(60).mean()
    tail = df.tail(n)
    points = [{"date": str(r["date"]), "close": round(float(r["close"]), 2),
               "ma20": round(float(r["ma20"]), 2) if pd.notna(r["ma20"]) else None,
               "ma60": round(float(r["ma60"]), 2) if pd.notna(r["ma60"]) else None}
              for _, r in tail.iterrows()]
    return {"code": code, "points": points}


def amount_trend(days: int = 20) -> dict:
    if not store.has_table("daily_bar"):
        return {"series": []}
    df = store.query("SELECT date, sum(amount) amt FROM daily_bar GROUP BY date ORDER BY date")
    if df.empty:
        return {"series": []}
    tail = df.tail(days)
    series = [{"date": str(r["date"]), "amount": round(float(r["amt"]) / 1e8, 2)}
              for _, r in tail.iterrows()]
    return {"series": series}
```

- [ ] **Step 4: 运行确认通过 + 提交**

Run: `python3 -m pytest tests/test_macro_kandapan.py -v`
Expected: PASS（3 tests）

```bash
git add aquant/macro.py tests/test_macro_kandapan.py
git commit -m "feat(macro): 研判/指数走势(收盘+MA20/60)/两市量能 领域函数

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 看大盘端点

**Files:**
- Modify: `server/schemas/macro.py`, `server/routers/macro.py`
- Test: `tests/test_macro_api.py`（追加）

**Interfaces:**
- Consumes: `aquant.macro.regime/index_series/amount_trend`
- Produces：
  - `GET /api/cockpit/regime` → `RegimeResp{state,score,suggested_position,note}`（`breadth`/`index` 嵌套 dict 一并透传，schema 用宽松 dict 承载额外字段）
  - `GET /api/cockpit/index-series?code=sh000300&n=120` → `IndexSeriesResp{code, points:list[dict]}`
  - `GET /api/cockpit/amount-trend?days=20` → `AmountTrendResp{series:list[dict]}`

- [ ] **Step 1: 写失败测试**

`tests/test_macro_api.py` 追加：
```python
def test_regime_endpoint(client, seed_db, monkeypatch):
    from aquant import macro
    monkeypatch.setattr(macro, "regime", lambda: {"state": "均衡", "score": 3, "suggested_position": "5成", "note": "x"})
    r = client.get("/api/cockpit/regime")
    assert r.status_code == 200 and r.json()["state"] == "均衡"


def test_index_series_endpoint(client, seed_db, monkeypatch):
    from aquant import macro
    monkeypatch.setattr(macro, "index_series",
                        lambda code="sh000300", n=120: {"code": code, "points": [{"date": "2026-06-23", "close": 3900, "ma20": 3890, "ma60": 3850}]})
    r = client.get("/api/cockpit/index-series?code=sh000300&n=60")
    assert r.status_code == 200 and r.json()["points"][0]["close"] == 3900


def test_amount_trend_endpoint(client, seed_db, monkeypatch):
    from aquant import macro
    monkeypatch.setattr(macro, "amount_trend", lambda days=20: {"series": [{"date": "2026-06-23", "amount": 9000.0}]})
    r = client.get("/api/cockpit/amount-trend?days=5")
    assert r.status_code == 200 and r.json()["series"][0]["amount"] == 9000.0
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/test_macro_api.py -k "regime or index_series or amount_trend" -v`
Expected: FAIL（404）

- [ ] **Step 3: 写 schema + 路由**

`server/schemas/macro.py` 追加：
```python
class RegimeResp(BaseModel):
    state: str
    score: float | int
    suggested_position: str | None = None
    note: str | None = None
    breadth: dict | None = None
    index: dict | None = None


class IndexSeriesResp(BaseModel):
    code: str
    points: list[dict]


class AmountTrendResp(BaseModel):
    series: list[dict]
```

`server/routers/macro.py` 追加（顶部 import 补新 schema）：
```python
from server.schemas.macro import RegimeResp, IndexSeriesResp, AmountTrendResp


@router.get("/regime", response_model=RegimeResp)
def regime() -> RegimeResp:
    d = macro.regime()
    return RegimeResp(**d) if d else RegimeResp(state="", score=0)


@router.get("/index-series", response_model=IndexSeriesResp)
def index_series(code: str = "sh000300", n: int = 120) -> IndexSeriesResp:
    return IndexSeriesResp(**macro.index_series(code=code, n=n))


@router.get("/amount-trend", response_model=AmountTrendResp)
def amount_trend(days: int = 20) -> AmountTrendResp:
    return AmountTrendResp(**macro.amount_trend(days=days))
```

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `python3 -m pytest tests/test_macro_api.py -v && python3 -m pytest -q`
Expected: 新 3 用例 PASS；全套全绿。

- [ ] **Step 5: 提交**

```bash
git add server/schemas/macro.py server/routers/macro.py tests/test_macro_api.py
git commit -m "feat(macro): 看大盘端点 regime/index-series/amount-trend

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec 覆盖**（对照 v3.1 第 1 节后端）：
- 研判 regime → Task 1/2。✅
- 指数走势+MA20/60（C 档：收盘线，无OHLC）→ `index_series` + `/index-series`（Task 1/2）。✅
- 两市量能（成交额趋势）→ `amount_trend` + `/amount-trend`（Task 1/2）。✅
- 只读库、无联网；其余端点复用不改。✅

**占位符扫描**：无 TBD/TODO；每步含完整代码。✅

**类型一致性**：`regime/index_series/amount_trend` 领域返回结构与端点 `response_model`（RegimeResp/IndexSeriesResp/AmountTrendResp）一致；index_series `{code,points}`、amount_trend `{series}` 前后一致；测试 mock 与真实签名一致。✅

**范围**：单一可测后端子系统（pytest 全绿）。前端为独立 Plan B。✅

**已知前提**：
- `index_series` MA 用 `rolling(20/60)`，历史<窗口时对应 ma 为 None（前端图上该段不画）。
- `amount_trend` 依赖 `daily_bar.amount`；seed_db fixture 已含 amount。
- `regime` 端点在 `market.regime()` 返回 `{}`（数据不足）时降级为 `{state:"",score:0}`，前端据空态处理。
