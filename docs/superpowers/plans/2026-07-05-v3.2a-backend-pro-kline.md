# v3.2A — 专业K线后端（MA/MACD + chart 端点）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 后端提供专业K线数据：`GET /api/stock/{code}/chart` 返回 bars(OHLC+量) + 均线(MA5/10/20/60) + MACD(DIF/DEA/hist)，供前端画主图K线+均线、副图量、副图MACD。

**Architecture:** 新 `aquant/chart.py` 纯领域函数（只读 `daily_bar`，rolling MA + ewm MACD）；FastAPI 挂既有 `server/routers/stock.py`。

**Tech Stack:** Python 3.11 · FastAPI · DuckDB · pandas · pytest + httpx TestClient。

## Global Constraints

- Python 3.11；新增逻辑放 `aquant/chart.py`。
- DB 路径由 `AQUANT_DATA_DIR` 驱动；测试在 import 任何 aquant 前设置（沿用 `tests/conftest.py`）。
- API 请求处理内**禁止**第三方网络；只读 `store`。
- 端点 `/api/stock/{code}/chart`；pydantic 响应模型；挂既有 `server/routers/stock.py`。
- 保留既有 `/api/stock/{code}/kline`（board 迷你线用）不动。
- 提交信息结尾加：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 在分支 `v3.2a-backend` 上开发；每 Task 末尾提交。
- 复用契约（已核对）：
  - `daily_bar` 列含 `date, open, high, low, close, volume`。
  - MACD EMA 写法（参考 `aquant/signal/timing.py:macd_signal`）：`dif = ema12 - ema26`（`c.ewm(span=12/26, adjust=False).mean()`），`dea = dif.ewm(span=9, adjust=False).mean()`，`hist = (dif - dea) * 2`。
  - `aquant.data.store.query/has_table`。

---

### Task 1: chart.py — stock_chart(MA/MACD)

**Files:**
- Create: `aquant/chart.py`, `tests/test_chart.py`

**Interfaces:**
- Consumes: `store`（读 `daily_bar`）
- Produces：
  - `aquant.chart.stock_chart(code: str, n: int = 250) -> dict`：
    ```
    {code, bars:[{date,open,high,low,close,volume}],
     ma:{ma5:[..],ma10:[..],ma20:[..],ma60:[..]},
     macd:{dif:[..],dea:[..],hist:[..]}}
    ```
    - 在**全序列**上算 MA/MACD 后取尾 n（保证尾段 MA/MACD 正确）；对齐 bars 长度；NaN → None（前段不足）。
    - 无数据/无表 → `{code, bars:[], ma:{...空列表}, macd:{...空列表}}`。数值 round 到 2（价/量除外量取整）。

- [ ] **Step 1: 写失败测试**

`tests/test_chart.py`：
```python
def test_stock_chart_shape(seed_db):
    from aquant import chart
    out = chart.stock_chart("600000", n=30)
    assert out["code"] == "600000"
    assert len(out["bars"]) == 30
    b = out["bars"][0]
    assert set(["date", "open", "high", "low", "close", "volume"]) == set(b)
    # MA/MACD 各序列与 bars 等长
    for k in ("ma5", "ma10", "ma20", "ma60"):
        assert len(out["ma"][k]) == 30
    for k in ("dif", "dea", "hist"):
        assert len(out["macd"][k]) == 30
    # fixture 80 日 → 尾段 MA60 有值
    assert out["ma"]["ma60"][-1] is not None
    # 前段 MA5 首值不足窗口 → None
    assert out["ma"]["ma5"][0] is None


def test_stock_chart_empty(seed_db):
    from aquant import chart
    out = chart.stock_chart("999999", n=30)
    assert out["bars"] == [] and out["macd"]["dif"] == []
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/test_chart.py -v`
Expected: FAIL（`ModuleNotFoundError: aquant.chart`）

- [ ] **Step 3: 实现 `chart.py`**

`aquant/chart.py`：
```python
"""专业K线数据：K线(OHLC+量) + 均线(MA5/10/20/60) + MACD(DIF/DEA/hist)。只读库。"""
from __future__ import annotations

import pandas as pd

from .data import store


def _col(series: pd.Series) -> list:
    return [round(float(v), 2) if pd.notna(v) else None for v in series]


def stock_chart(code: str, n: int = 250) -> dict:
    empty = {"code": code, "bars": [],
             "ma": {"ma5": [], "ma10": [], "ma20": [], "ma60": []},
             "macd": {"dif": [], "dea": [], "hist": []}}
    if not store.has_table("daily_bar"):
        return empty
    df = store.query(
        "SELECT date, open, high, low, close, volume FROM daily_bar WHERE code = ? ORDER BY date", [code])
    if df.empty:
        return empty
    c = df["close"]
    for w in (5, 10, 20, 60):
        df[f"ma{w}"] = c.rolling(w).mean()
    ema12 = c.ewm(span=12, adjust=False).mean()
    ema26 = c.ewm(span=26, adjust=False).mean()
    dif = ema12 - ema26
    dea = dif.ewm(span=9, adjust=False).mean()
    hist = (dif - dea) * 2
    df["dif"], df["dea"], df["hist"] = dif, dea, hist
    t = df.tail(n)
    bars = [{"date": str(r["date"]), "open": round(float(r["open"]), 2),
             "high": round(float(r["high"]), 2), "low": round(float(r["low"]), 2),
             "close": round(float(r["close"]), 2), "volume": int(r["volume"])}
            for _, r in t.iterrows()]
    return {
        "code": code, "bars": bars,
        "ma": {"ma5": _col(t["ma5"]), "ma10": _col(t["ma10"]), "ma20": _col(t["ma20"]), "ma60": _col(t["ma60"])},
        "macd": {"dif": _col(t["dif"]), "dea": _col(t["dea"]), "hist": _col(t["hist"])},
    }
```

- [ ] **Step 4: 运行确认通过 + 提交**

Run: `python3 -m pytest tests/test_chart.py -v`
Expected: PASS（2 tests）

```bash
git add aquant/chart.py tests/test_chart.py
git commit -m "feat(chart): 专业K线数据 stock_chart(OHLC+MA5/10/20/60+MACD)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: /api/stock/{code}/chart 端点

**Files:**
- Modify: `server/schemas/stock.py`, `server/routers/stock.py`
- Test: `tests/test_stock_api.py`（追加）

**Interfaces:**
- Consumes: `aquant.chart.stock_chart`
- Produces：`GET /api/stock/{code}/chart?n=250` → `ChartResp{code, bars:list[dict], ma:dict, macd:dict}`

- [ ] **Step 1: 写失败测试**

`tests/test_stock_api.py` 追加：
```python
def test_chart_endpoint(client, seed_db):
    r = client.get("/api/stock/600000/chart?n=20")
    assert r.status_code == 200
    body = r.json()
    assert body["code"] == "600000"
    assert len(body["bars"]) == 20
    assert set(["ma5", "ma10", "ma20", "ma60"]).issubset(body["ma"])
    assert set(["dif", "dea", "hist"]).issubset(body["macd"])
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/test_stock_api.py::test_chart_endpoint -v`
Expected: FAIL（404）

- [ ] **Step 3: 写 schema + 路由**

`server/schemas/stock.py` 追加：
```python
class ChartResp(BaseModel):
    code: str
    bars: list[dict]
    ma: dict
    macd: dict
```

`server/routers/stock.py` 追加（顶部 import 补 `from aquant import chart` 与 `ChartResp`）：
```python
from aquant import chart
from server.schemas.stock import ChartResp


@router.get("/{code}/chart", response_model=ChartResp)
def stock_chart(code: str, n: int = 250) -> ChartResp:
    return ChartResp(**chart.stock_chart(code, n=n))
```
（注意：`server/routers/stock.py` 已有 `router = APIRouter(prefix="/api/stock")`，新路由挂其下即 `/api/stock/{code}/chart`。）

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `python3 -m pytest tests/test_stock_api.py -v && python3 -m pytest -q`
Expected: chart 端点 PASS；全套全绿。

- [ ] **Step 5: 提交**

```bash
git add server/schemas/stock.py server/routers/stock.py tests/test_stock_api.py
git commit -m "feat(chart): 个股专业K线端点 /api/stock/{code}/chart

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec 覆盖**（对照 v3.2 第 1 节后端）：
- stock_chart：bars(OHLC+量) + MA5/10/20/60 + MACD(DIF/DEA/hist) → Task 1。✅
- 全序列算 MA/MACD 后取尾 n、NaN→None → Task 1。✅
- `/api/stock/{code}/chart` 端点 → Task 2。✅
- 保留既有 /kline 不动；只读库、无联网。✅

**占位符扫描**：无 TBD/TODO；每步完整代码。✅

**类型一致性**：`stock_chart` 返回 `{code,bars,ma,macd}` 与 `ChartResp` 字段一致；MACD EMA(12/26/9)、hist=(dif-dea)*2 与 timing 一致；测试断言与结构一致。✅

**范围**：单一可测后端子系统（pytest 全绿）。前端为独立 Plan B。✅

**已知前提**：
- MA/MACD 在全序列上算再取尾 n，保证尾段指标正确（不因截断失真）；fixture 80 日足够验证 MA60 尾值非空。
- `volume` 取整（int）；价/MA/MACD round 2。
- 无数据返回空结构（bars=[]），前端据空态处理。
