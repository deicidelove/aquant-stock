# Plan v3.3A 后端龙虎榜（TDD）

分支：`v3.3a-lhb-backend`（从 main 切）。每任务先写测试→RED→实现→GREEN。

## Task 1 — 席位分类器 + HOTMONEY 词典（纯函数，无网络）
- 文件：`aquant/lhb.py` 新增 `classify_seat(name)-> (seat_type, hotmoney_name|None)`；`HOTMONEY` dict（关键字→别名，内置：章盟主/赵老哥/佛山无影脚/炒股养家等 6-8 条常见+量化席位）。
- 测试 `tests/test_lhb_classify.py`：机构专用→inst；深股通专用→north；命中词典→hotmoney+别名；普通营业部→normal,None。

## Task 2 — 域层读取（只读 DuckDB，offline）
- `aquant/lhb.py`：`lhb_today(limit=50)`、`lhb_stock(code,date=None)`。用 store.query 读 lhb_detail/lhb_seat。
- tags 推断：读该股席位 seat_type 聚合。
- 测试 `tests/test_lhb_domain.py`：conftest 造 lhb_detail + lhb_seat 假数据 → today 按 net_buy 降序、含 tags；stock 返回 buy/sell 列表且 seat_type 正确；无数据返回空结构不报错。
- conftest：新增 lhb_detail/lhb_seat DROP 清理。

## Task 3 — source 席位函数
- `aquant/data/sources/akshare_source.py`：`lhb_seats(code,date,flag)` 包 `stock_lhb_stock_detail_em`，rename 中文列→英文，加 @_robust，date 转 YYYYMMDD 入参。
- 测试 `tests/test_lhb_source.py`：monkeypatch ak.stock_lhb_stock_detail_em 返回假 df → 断言列名映射（seat/buy/sell/net）、code zfill。

## Task 4 — refresh 任务 + store 键
- `aquant/data/store.py`：TABLE_KEYS 加 `"lhb_seat": ["code","date","side","seat"]`。
- `server/refresh/lhb.py`：`refresh_lhb(days=1)` 抓 detail 存表；对最新日每 code 抓买/卖席位存 lhb_seat。
- scheduler `_eod_job` 追加 `from server.refresh.lhb import refresh_lhb; refresh_lhb()`。
- 测试 `tests/test_refresh_lhb.py`：monkeypatch source（detail + seats）→ refresh_lhb 后 store 有 lhb_detail/lhb_seat 行；side/net 正确。

## Task 5 — API router + schemas
- `server/schemas/lhb.py`：LhbTodayResp{date,rows:[LhbRow]}、LhbStockResp{code,name,date,reason,buy:[Seat],sell:[Seat]}。
- `server/routers/lhb.py`：GET /api/lhb/today、GET /api/lhb/stock/{code}，调 lhb 域层。app.py 注册 router。
- 测试 `tests/test_api_lhb.py`（TestClient）：造数据→ /api/lhb/today 200 且 rows 降序；/api/lhb/stock/{code} 200 含 buy/sell；请求路径不发网络（无 monkeypatch source 也能跑）。

## 收尾
- 全后端测试绿（当前 64，+≈10）。
- WORKLOG 追加；commit（trailer）；不单独 push，等 B2 一起或本阶段 finishing-a-development-branch 合并 main 后 push。
