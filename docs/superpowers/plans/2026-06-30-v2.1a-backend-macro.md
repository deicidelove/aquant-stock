# v2.1A — 驾驶舱宏观后端实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为驾驶舱宏观重做提供后端：板块资金流入库、盘中资金刷新、市场情绪/大盘资金/板块资金/异常资金 领域函数与端点，全部只读库/请求内不联网。

**Architecture:** 新增 akshare 行业资金流源 + `sector_fund_flow` 表 + 刷新任务；新增 `aquant/macro.py` 领域层（情绪/大盘资金趋势/板块资金排行/异常资金 z-score/多指数）复用 `fund_flow`/`sector_fund_flow`/`daily_bar`/`index_daily`；FastAPI `/api/cockpit/*` 宏观端点薄封装。

**Tech Stack:** Python 3.11 · FastAPI · DuckDB(`aquant.data.store`) · pandas · pytest + httpx TestClient。

## Global Constraints

- Python 3.11；复用现有领域核心；新增宏观逻辑放 `aquant/macro.py`，资金源放 `aquant/data/sources/akshare_source.py`。
- DB 路径由 `AQUANT_DATA_DIR` 驱动；测试在 import 任何 aquant 前设置（沿用 `tests/conftest.py`）。
- 新表在 `store.TABLE_KEYS` 注册主键；`store.save` 用 INSERT OR REPLACE 幂等 upsert。
- API 请求处理与刷新任务线程内禁止第三方网络（刷新任务自身是后台采集，允许联网；端点只读库）。**端点只读 `store`，绝不调用 `akshare`。**
- 端点路径 `/api/cockpit/*`，pydantic 响应模型。
- 北向资金不做（无可靠实时源）。
- 提交信息结尾加：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 在分支 `v2.1a-backend` 上开发；每 Task 末尾提交。
- 复用契约（已核对）：
  - `ak.stock_sector_fund_flow_rank(indicator="今日", sector_type="行业资金流")` → 列含 `名称/今日涨跌幅/今日主力净流入-净额/今日主力净流入-净占比/今日主力净流入最大股`（约 80–500 行）。
  - `aquant.data.sources.akshare_source` 有 `fund_flow_rank(indicator="今日")` → `[code,name,close,pct_chg,main_net,main_net_pct,...]`；`industry_snapshot()`；`index_daily(index_code)` → `[code,date,close]`；`spot_snapshot()`。装饰器 `@_robust`、模块级 `ak`。
  - `aquant.market.breadth() -> {total,up,down,limit_up,up_ratio,above_ma20_pct,above_ma60_pct}`；`index_trend(code="sh000300") -> {code,close,above_ma20,above_ma60,ret_20d,ret_60d}`。
  - `fund_flow` 表：`[code,name,close,pct_chg,main_net,main_net_pct,date]`，主键 `[code,date]`。
  - `aquant.data.store.save/query/has_table/max_date`。

---

### Task 1: 行业资金流源 + 表 + 刷新

**Files:**
- Modify: `aquant/data/sources/akshare_source.py`（新增 `sector_fund_flow`）
- Modify: `aquant/data/store.py`（`TABLE_KEYS` 加 `sector_fund_flow`）
- Create: `server/refresh/fundflow.py`（`refresh_sector_fund_flow`）
- Test: `tests/test_sector_fund_flow.py`

**Interfaces:**
- Produces:
  - `akshare_source.sector_fund_flow() -> pd.DataFrame`，列 `[sector, pct_chg, main_net, main_net_pct, leader]`
  - `server.refresh.fundflow.refresh_sector_fund_flow(fetch=None) -> int`（写 `sector_fund_flow` 表，列同上 + `date`=今日；按 `[sector,date]` upsert，盘中重复跑覆盖当日；返回行数）

- [ ] **Step 1: 注册表主键**

`aquant/data/store.py` 的 `TABLE_KEYS` 新增：
```python
    "sector_fund_flow": ["sector", "date"],
```

- [ ] **Step 2: 写失败测试（mock ak）**

`tests/test_sector_fund_flow.py`：
```python
import pandas as pd


def test_sector_fund_flow_source_normalizes(monkeypatch):
    from aquant.data.sources import akshare_source as src
    raw = pd.DataFrame({
        "名称": ["医药生物", "煤炭"], "今日涨跌幅": [4.94, -0.8],
        "今日主力净流入-净额": [6.33e9, -1.2e9], "今日主力净流入-净占比": [3.98, -1.0],
        "今日主力净流入最大股": ["恒瑞医药", "中国神华"],
    })
    monkeypatch.setattr(src.ak, "stock_sector_fund_flow_rank", lambda **k: raw)
    df = src.sector_fund_flow()
    assert list(df.columns) == ["sector", "pct_chg", "main_net", "main_net_pct", "leader"]
    assert df.loc[df["sector"] == "医药生物", "main_net"].iloc[0] == 6.33e9


def test_refresh_sector_fund_flow_writes(seed_db):
    from server.refresh import fundflow
    fake = pd.DataFrame({"sector": ["医药生物"], "pct_chg": [4.94], "main_net": [6.33e9],
                         "main_net_pct": [3.98], "leader": ["恒瑞医药"]})
    n = fundflow.refresh_sector_fund_flow(fetch=lambda: fake)
    assert n == 1
    rows = seed_db.query("SELECT sector, main_net, date FROM sector_fund_flow")
    assert rows["sector"].iloc[0] == "医药生物" and rows["date"].notna().all()
```

- [ ] **Step 3: 运行确认失败**

Run: `python3 -m pytest tests/test_sector_fund_flow.py -v`
Expected: FAIL（`AttributeError: ...'sector_fund_flow'` / `No module named 'server.refresh.fundflow'`）

- [ ] **Step 4: 实现源 + 刷新**

`aquant/data/sources/akshare_source.py` 在 `industry_snapshot` 附近追加：
```python
_SECTOR_FLOW_MAP = {
    "名称": "sector", "今日涨跌幅": "pct_chg", "今日主力净流入-净额": "main_net",
    "今日主力净流入-净占比": "main_net_pct", "今日主力净流入最大股": "leader",
}


@_robust
def sector_fund_flow() -> pd.DataFrame:
    """行业板块资金流：各板块今日主力净额/净占比/领涨股。columns=[sector,pct_chg,main_net,main_net_pct,leader]。"""
    df = ak.stock_sector_fund_flow_rank(indicator="今日", sector_type="行业资金流").rename(columns=_SECTOR_FLOW_MAP)
    keep = [c for c in _SECTOR_FLOW_MAP.values() if c in df.columns]
    out = df[keep].copy()
    for c in ("pct_chg", "main_net", "main_net_pct"):
        if c in out.columns:
            out[c] = pd.to_numeric(out[c], errors="coerce")
    return out
```

`server/refresh/fundflow.py`：
```python
"""资金流刷新任务：行业板块资金、全市场个股资金 → 落 DuckDB。后台运行。"""
from __future__ import annotations

from datetime import date

from aquant.data import store
from aquant.data.sources import akshare_source as src


def refresh_sector_fund_flow(fetch=None) -> int:
    """行业板块资金流入库 sector_fund_flow（按 [sector,date] upsert，当日覆盖）。"""
    fetch = fetch or src.sector_fund_flow
    df = fetch()
    if df is None or df.empty:
        return 0
    df = df.copy()
    df["date"] = date.today().isoformat()
    return store.save("sector_fund_flow", df)
```

- [ ] **Step 5: 运行确认通过 + 提交**

Run: `python3 -m pytest tests/test_sector_fund_flow.py -v`
Expected: PASS（2 tests）

```bash
git add aquant/data/sources/akshare_source.py aquant/data/store.py server/refresh/fundflow.py tests/test_sector_fund_flow.py
git commit -m "feat(macro): 行业资金流源 + sector_fund_flow 表 + 刷新

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 全市场个股资金盘中刷新 + 调度接入

**Files:**
- Modify: `server/refresh/fundflow.py`（新增 `refresh_fund_flow`）
- Modify: `server/refresh/scheduler.py`（盘中 job 加入两个资金刷新）
- Test: `tests/test_sector_fund_flow.py`（追加）, `tests/test_scheduler.py`（追加注册断言）

**Interfaces:**
- Consumes: `akshare_source.fund_flow_rank()`
- Produces:
  - `server.refresh.fundflow.refresh_fund_flow(fetch=None) -> int`（写 `fund_flow` 表，列 `[code,name,close,pct_chg,main_net,main_net_pct]` + `date`=今日；按 `[code,date]` upsert；返回行数）
  - `scheduler._intraday_job` 额外调用 `refresh_sector_fund_flow`、`refresh_fund_flow`（异常各自吞掉，不互相阻塞）

- [ ] **Step 1: 写失败测试（追加）**

`tests/test_sector_fund_flow.py` 追加：
```python
def test_refresh_fund_flow_writes(seed_db):
    from server.refresh import fundflow
    fake = pd.DataFrame({"code": ["600000"], "name": ["浦发"], "close": [10.0],
                         "pct_chg": [1.2], "main_net": [1.5e8], "main_net_pct": [3.0]})
    n = fundflow.refresh_fund_flow(fetch=lambda: fake)
    assert n == 1
    rows = seed_db.query("SELECT code, main_net, date FROM fund_flow")
    assert rows["code"].iloc[0] == "600000" and rows["date"].notna().all()
```

`tests/test_scheduler.py` 追加：
```python
def test_intraday_registers_fundflow():
    import server.refresh.scheduler as sch
    # _intraday_job 引用了资金刷新函数（通过模块属性可见）
    import server.refresh.fundflow as ff
    assert hasattr(ff, "refresh_sector_fund_flow") and hasattr(ff, "refresh_fund_flow")
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/test_sector_fund_flow.py::test_refresh_fund_flow_writes -v`
Expected: FAIL（`AttributeError: ...'refresh_fund_flow'`）

- [ ] **Step 3: 实现 + 接调度**

`server/refresh/fundflow.py` 追加：
```python
def refresh_fund_flow(fetch=None) -> int:
    """全市场个股资金流入库 fund_flow（按 [code,date] upsert，当日覆盖）。"""
    fetch = fetch or src.fund_flow_rank
    df = fetch()
    if df is None or df.empty:
        return 0
    keep = [c for c in ("code", "name", "close", "pct_chg", "main_net", "main_net_pct") if c in df.columns]
    df = df[keep].copy()
    df["date"] = date.today().isoformat()
    return store.save("fund_flow", df)
```

`server/refresh/scheduler.py` 的 `_intraday_job` 改为同时刷资金（导入 fundflow）：
```python
from server.refresh import snapshots, scores, fundflow
...
def _intraday_job() -> None:
    if not is_trading_hours(datetime.now()):
        return
    for fn in (snapshots.refresh_quotes, snapshots.refresh_sectors,
               fundflow.refresh_sector_fund_flow, fundflow.refresh_fund_flow):
        try:
            fn()
        except Exception:  # noqa: BLE001
            pass
```
（保留原有 import 与 eod_job 不变；只把两资金刷新加入盘中循环。）

- [ ] **Step 4: 运行确认通过**

Run: `python3 -m pytest tests/test_sector_fund_flow.py tests/test_scheduler.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add server/refresh/fundflow.py server/refresh/scheduler.py tests/test_sector_fund_flow.py tests/test_scheduler.py
git commit -m "feat(macro): 个股资金盘中刷新 + 调度接入两资金任务

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: 宏观领域 — 市场情绪 + 大盘资金 + 多指数

**Files:**
- Create: `aquant/macro.py`, `tests/test_macro.py`
- Modify: 无

**Interfaces:**
- Consumes: `aquant.market.breadth`、`index_trend`；`store`（读 `fund_flow`、`daily_bar`）
- Produces：
  - `aquant.macro.sentiment() -> dict`：`{up,down,limit_up,limit_down,amount,score,label}`（`score` 0–100 温度，`label` 冰点/偏冷/中性/偏热/过热）。涨跌停近似：`pct_chg>=9.8` 涨停、`<=-9.8` 跌停（来自每只最新日线行）。`amount` = 最新日全市场成交额合计。
  - `market_fund_trend(days: int = 10) -> dict`：`{today, series:[{date,net}]}`，`net` = 当日 `fund_flow.main_net` 合计（亿元，/1e8 保留 2 位）。
  - `indices(codes: list[str] | None = None) -> list[dict]`：对每个指数调 `market.index_trend`，跳过无数据；默认 `["sh000300","sh000001","sz399006"]`。

- [ ] **Step 1: 写失败测试**

`tests/test_macro.py`：
```python
import pandas as pd


def _seed_fund_flow(store, rows):
    store.save("fund_flow", pd.DataFrame(rows))


def test_sentiment_shape(seed_db):
    from aquant import macro
    s = macro.sentiment()
    assert set(["up", "down", "limit_up", "limit_down", "amount", "score", "label"]).issubset(s)
    assert 0 <= s["score"] <= 100


def test_market_fund_trend(seed_db):
    from aquant import macro
    _seed_fund_flow(seed_db, [
        {"code": "600000", "name": "浦发", "close": 10, "pct_chg": 1, "main_net": 1e8, "main_net_pct": 1, "date": "2026-06-22"},
        {"code": "000001", "name": "平安", "close": 20, "pct_chg": 1, "main_net": -3e8, "main_net_pct": -1, "date": "2026-06-22"},
        {"code": "600000", "name": "浦发", "close": 10, "pct_chg": 1, "main_net": 2e8, "main_net_pct": 1, "date": "2026-06-23"},
    ])
    t = macro.market_fund_trend(days=10)
    assert "today" in t and isinstance(t["series"], list)
    last = [p for p in t["series"] if p["date"] == "2026-06-23"][0]
    assert round(last["net"], 2) == round(2e8 / 1e8, 2)  # 2 亿


def test_indices_skips_missing(seed_db):
    from aquant import macro
    out = macro.indices(["sh000300", "nope999"])
    assert isinstance(out, list)  # 无数据的跳过，不报错
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/test_macro.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'aquant.macro'`）

- [ ] **Step 3: 实现 `macro.py`**

`aquant/macro.py`：
```python
"""宏观盘面：市场情绪、大盘资金趋势、多指数。只读 DuckDB，无第三方联网。"""
from __future__ import annotations

import pandas as pd

from . import market
from .data import store

_INDEX_DEFAULTS = ["sh000300", "sh000001", "sz399006"]


def _latest_rows() -> pd.DataFrame:
    """每只股票最新一日的 close/pct_chg/amount。"""
    if not store.has_table("daily_bar"):
        return pd.DataFrame()
    return store.query(
        "SELECT code, pct_chg, amount FROM ("
        "  SELECT code, pct_chg, amount, row_number() OVER "
        "    (PARTITION BY code ORDER BY date DESC) rn FROM daily_bar) t WHERE rn = 1")


def sentiment() -> dict:
    b = market.breadth()
    df = _latest_rows()
    limit_down = int((df["pct_chg"] <= -9.8).sum()) if not df.empty else 0
    amount = float(df["amount"].sum()) if not df.empty and "amount" in df else 0.0
    up, down = b.get("up", 0), b.get("down", 0)
    limit_up = b.get("limit_up", 0)
    total = max(b.get("total", 0), 1)
    # 温度 0~100：上涨占比为主 + 涨停溢价 − 跌停惩罚
    up_ratio = up / total
    score = max(0.0, min(100.0, up_ratio * 100 + (limit_up - limit_down) / total * 200))
    label = ("过热" if score >= 80 else "偏热" if score >= 60 else
             "中性" if score >= 40 else "偏冷" if score >= 20 else "冰点")
    return {"up": up, "down": down, "limit_up": limit_up, "limit_down": limit_down,
            "amount": round(amount, 2), "score": round(score, 1), "label": label}


def market_fund_trend(days: int = 10) -> dict:
    if not store.has_table("fund_flow"):
        return {"today": 0.0, "series": []}
    df = store.query("SELECT date, sum(main_net) net FROM fund_flow GROUP BY date ORDER BY date")
    if df.empty:
        return {"today": 0.0, "series": []}
    df["net"] = (df["net"] / 1e8).round(2)
    tail = df.tail(days)
    series = [{"date": str(r["date"]), "net": float(r["net"])} for _, r in tail.iterrows()]
    return {"today": float(tail["net"].iloc[-1]), "series": series}


def indices(codes: list[str] | None = None) -> list[dict]:
    out = []
    for c in (codes or _INDEX_DEFAULTS):
        t = market.index_trend(c)
        if t:
            out.append(t)
    return out
```

- [ ] **Step 4: 运行确认通过 + 提交**

Run: `python3 -m pytest tests/test_macro.py -v`
Expected: PASS（3 tests）

```bash
git add aquant/macro.py tests/test_macro.py
git commit -m "feat(macro): 市场情绪 + 大盘资金趋势 + 多指数

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 宏观领域 — 板块资金排行 + 异常资金

**Files:**
- Modify: `aquant/macro.py`（新增 `sector_fund_rank`、`abnormal_fund`）
- Test: `tests/test_macro.py`（追加）

**Interfaces:**
- Consumes: `store`（读 `sector_fund_flow`、`fund_flow`）
- Produces：
  - `sector_fund_rank(top: int = 20) -> dict`：`{as_of, rows:[{sector,pct_chg,main_net,main_net_pct,leader}]}`，读最新 date，按 `main_net` 降序，取首尾各 `top`（净流入榜 + 净流出榜合并返回全部排序行的前后段；实现上返回按 main_net 降序的全部，前端切分）。简化：返回最新 date 全部行按 main_net 降序。
  - `abnormal_fund(scope: str = "stock", n: int = 20, z: float = 2.0, top: int = 20) -> dict`：`{scope, rows:[{key, latest, mean, std, z}]}`。`scope="stock"` 读 `fund_flow`（key=code）、`"sector"` 读 `sector_fund_flow`（key=sector）；对每个 key 取按 date 排序的 `main_net` 序列，用**除最新外**的近 `n` 个算 mean/std，`z=(latest-mean)/std`；保留 `abs(z)>=z阈值` 且 std>0，按 `abs(z)` 降序取 `top`。历史不足（<3 点）的 key 跳过。

- [ ] **Step 1: 写失败测试**

`tests/test_macro.py` 追加：
```python
def test_sector_fund_rank(seed_db):
    from aquant import macro
    seed_db.save("sector_fund_flow", pd.DataFrame([
        {"sector": "医药", "pct_chg": 2.0, "main_net": 5e8, "main_net_pct": 1.0, "leader": "恒瑞", "date": "2026-06-23"},
        {"sector": "煤炭", "pct_chg": -1.0, "main_net": -2e8, "main_net_pct": -1.0, "leader": "神华", "date": "2026-06-23"},
    ]))
    r = macro.sector_fund_rank()
    assert r["as_of"] == "2026-06-23"
    assert r["rows"][0]["sector"] == "医药"  # main_net 降序


def test_abnormal_fund_stock(seed_db):
    from aquant import macro
    # 600000 近几日 main_net 平稳，最新一日暴增 → 异常
    rows = []
    for i, d in enumerate(["2026-06-17", "2026-06-18", "2026-06-19", "2026-06-22"]):
        rows.append({"code": "600000", "name": "浦发", "close": 10, "pct_chg": 1,
                     "main_net": 1e7 if d != "2026-06-22" else 5e8, "main_net_pct": 1, "date": d})
    seed_db.save("fund_flow", pd.DataFrame(rows))
    out = macro.abnormal_fund(scope="stock", n=5, z=2.0)
    assert out["scope"] == "stock"
    assert any(x["key"] == "600000" for x in out["rows"])
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/test_macro.py::test_abnormal_fund_stock -v`
Expected: FAIL（`AttributeError: ...'abnormal_fund'`）

- [ ] **Step 3: 实现**

`aquant/macro.py` 追加：
```python
def sector_fund_rank(top: int = 20) -> dict:  # noqa: ARG001 预留前端切分用
    if not store.has_table("sector_fund_flow"):
        return {"as_of": None, "rows": []}
    as_of = store.query("SELECT max(date) d FROM sector_fund_flow")["d"].iloc[0]
    if as_of is None:
        return {"as_of": None, "rows": []}
    df = store.query("SELECT sector,pct_chg,main_net,main_net_pct,leader FROM sector_fund_flow "
                     "WHERE date = ? ORDER BY main_net DESC", [as_of])
    return {"as_of": str(as_of), "rows": df.to_dict(orient="records")}


def abnormal_fund(scope: str = "stock", n: int = 20, z: float = 2.0, top: int = 20) -> dict:
    table, key = ("fund_flow", "code") if scope == "stock" else ("sector_fund_flow", "sector")
    if not store.has_table(table):
        return {"scope": scope, "rows": []}
    df = store.query(f"SELECT {key} k, date, main_net FROM {table} ORDER BY {key}, date")
    if df.empty:
        return {"scope": scope, "rows": []}
    out = []
    for k, g in df.groupby("k", sort=False):
        vals = g["main_net"].astype(float).tolist()
        if len(vals) < 3:
            continue
        latest = vals[-1]
        hist = vals[max(0, len(vals) - 1 - n):-1]  # 除最新外近 n 个
        if len(hist) < 2:
            continue
        s = pd.Series(hist)
        mean, std = float(s.mean()), float(s.std(ddof=0))
        if std <= 0:
            continue
        zz = (latest - mean) / std
        if abs(zz) >= z:
            out.append({"key": str(k), "latest": round(latest, 2), "mean": round(mean, 2),
                        "std": round(std, 2), "z": round(zz, 2)})
    out.sort(key=lambda x: abs(x["z"]), reverse=True)
    return {"scope": scope, "rows": out[:top]}
```

- [ ] **Step 4: 运行确认通过 + 提交**

Run: `python3 -m pytest tests/test_macro.py -v`
Expected: PASS（全部 macro 用例）

```bash
git add aquant/macro.py tests/test_macro.py
git commit -m "feat(macro): 板块资金排行 + 异常资金 z-score

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 驾驶舱宏观端点

**Files:**
- Create: `server/schemas/macro.py`, `server/routers/macro.py`, `tests/test_macro_api.py`
- Modify: `server/app.py`（include macro router）

**Interfaces:**
- Consumes: `aquant.macro`、`aquant.data.store`（读"更新于"时间戳）
- Produces：
  - `GET /api/cockpit/indices` → `{rows:[index_trend...]}`
  - `GET /api/cockpit/sentiment` → `{up,down,limit_up,limit_down,amount,score,label}`
  - `GET /api/cockpit/market-fund?days=10` → `{today, series:[{date,net}]}`
  - `GET /api/cockpit/sector-fund` → `{as_of, rows:[...]}`
  - `GET /api/cockpit/abnormal?scope=stock&n=20&z=2` → `{scope, rows:[...]}`

- [ ] **Step 1: 写失败测试**

`tests/test_macro_api.py`：
```python
import pandas as pd


def test_indices_endpoint(client, seed_db, monkeypatch):
    from aquant import macro
    monkeypatch.setattr(macro, "indices", lambda codes=None: [{"code": "sh000300", "close": 3900.0}])
    r = client.get("/api/cockpit/indices")
    assert r.status_code == 200 and r.json()["rows"][0]["code"] == "sh000300"


def test_sentiment_endpoint(client, seed_db):
    r = client.get("/api/cockpit/sentiment")
    assert r.status_code == 200
    assert "score" in r.json() and "label" in r.json()


def test_sector_fund_endpoint(client, seed_db):
    seed_db.save("sector_fund_flow", pd.DataFrame([
        {"sector": "医药", "pct_chg": 2.0, "main_net": 5e8, "main_net_pct": 1.0, "leader": "恒瑞", "date": "2026-06-23"}]))
    r = client.get("/api/cockpit/sector-fund")
    assert r.status_code == 200 and r.json()["rows"][0]["sector"] == "医药"


def test_abnormal_endpoint(client, seed_db):
    r = client.get("/api/cockpit/abnormal?scope=sector&n=5&z=2")
    assert r.status_code == 200 and r.json()["scope"] == "sector"
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m pytest tests/test_macro_api.py -v`
Expected: FAIL（404）

- [ ] **Step 3: 写 schema**

`server/schemas/macro.py`：
```python
from pydantic import BaseModel


class IndicesResp(BaseModel):
    rows: list[dict]


class SentimentResp(BaseModel):
    up: int
    down: int
    limit_up: int
    limit_down: int
    amount: float
    score: float
    label: str


class MarketFundResp(BaseModel):
    today: float
    series: list[dict]


class SectorFundResp(BaseModel):
    as_of: str | None
    rows: list[dict]


class AbnormalResp(BaseModel):
    scope: str
    rows: list[dict]
```

- [ ] **Step 4: 写路由**

`server/routers/macro.py`：
```python
from fastapi import APIRouter

from aquant import macro
from server.schemas.macro import IndicesResp, SentimentResp, MarketFundResp, SectorFundResp, AbnormalResp

router = APIRouter(prefix="/api/cockpit", tags=["macro"])


@router.get("/indices", response_model=IndicesResp)
def indices() -> IndicesResp:
    return IndicesResp(rows=macro.indices())


@router.get("/sentiment", response_model=SentimentResp)
def sentiment() -> SentimentResp:
    return SentimentResp(**macro.sentiment())


@router.get("/market-fund", response_model=MarketFundResp)
def market_fund(days: int = 10) -> MarketFundResp:
    return MarketFundResp(**macro.market_fund_trend(days=days))


@router.get("/sector-fund", response_model=SectorFundResp)
def sector_fund() -> SectorFundResp:
    return SectorFundResp(**macro.sector_fund_rank())


@router.get("/abnormal", response_model=AbnormalResp)
def abnormal(scope: str = "stock", n: int = 20, z: float = 2.0) -> AbnormalResp:
    return AbnormalResp(**macro.abnormal_fund(scope=scope, n=n, z=z))
```

- [ ] **Step 5: 注册路由**

`server/app.py` 的 routers import 行加入 `macro`，并加 `app.include_router(macro.router)`。

- [ ] **Step 6: 运行确认通过 + 全量回归**

Run: `python3 -m pytest tests/test_macro_api.py -v && python3 -m pytest -q`
Expected: macro API 4 用例 PASS；全套全绿。

- [ ] **Step 7: 提交**

```bash
git add server/schemas/macro.py server/routers/macro.py server/app.py tests/test_macro_api.py
git commit -m "feat(macro): 驾驶舱宏观端点（指数/情绪/大盘资金/板块资金/异常资金）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec 覆盖**（对照 v2.1 支柱① 宏观五模块 + ③ 活起来 后端部分）：
- 大盘指数 → `macro.indices` + `/indices`（Task 3/5）。✅
- 市场情绪（涨跌家数/涨停跌停/量能/温度）→ `macro.sentiment` + `/sentiment`（Task 3/5）。✅
- 大盘资金 → `macro.market_fund_trend` + `/market-fund`（Task 3/5）。✅
- 板块资金 → 新源+表+刷新（Task 1）+ `sector_fund_rank` + `/sector-fund`（Task 4/5）。✅
- 异常资金 → `abnormal_fund` z-score + `/abnormal`（Task 4/5）。✅
- 盘中活起来（资金落库）→ `refresh_fund_flow`/`refresh_sector_fund_flow` 接入盘中调度（Task 1/2）。✅
- 北向：不做（spec 已剔除）。✅
- 端点只读库、不联网；刷新任务后台采集。✅
- 注：前端"更新于 HH:MM"时间戳——sector_fund/quote 表已含 date/ts；情绪/指数的"更新于"由前端按 `latest_bar_date`/轮询时间展示，端点无需额外字段（Plan B 处理）。

**占位符扫描**：无 TBD/TODO；每步含完整可运行代码。✅

**类型一致性**：`sector_fund_flow`(源)→`refresh_sector_fund_flow`→`sector_fund_rank` 列一致；`refresh_fund_flow` 写列与 `fund_flow` 既有列一致；`sentiment/market_fund_trend/indices/sector_fund_rank/abnormal_fund` 返回结构与各 `response_model` 字段一致。✅

**范围**：单一可测后端子系统（pytest 全绿）。前端为独立 Plan B。✅

**已知前提/风险**：
- 异常资金依赖 `fund_flow`/`sector_fund_flow` 多日历史；历史不足时返回空或少量（测试用注入多日数据覆盖）。
- 板块资金/个股资金的盘中"今日"为东财累计值，按 `[*,date]` upsert 当日覆盖——既给历史（每日一行）又给盘中更新。
- `sentiment` 的温度公式为简单合成（spec 已列为"未决-可调"）；阈值分档先用 20/40/60/80。
- 端点 `client` fixture 依赖现有 conftest；`seed_db` 提供 daily_bar/stock_basic，资金类表由各测试注入。
