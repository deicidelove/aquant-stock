# v3.2B — 专业K线前端（三联图 + 接入个股页）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `/stock/:code` 的裸蜡烛升级为专业三联图：主图(K线+MA5/10/20/60) + 副图(成交量) + 副图(MACD)，十字光标三图联动 + 缩放；消费 v3.2A 的 `/api/stock/{code}/chart`。

**Architecture:** 沿用前端栈：client+hook 取 chart 数据 → 纯 option 函数 `buildProKlineOption` → `ProKlineChart`(EChart 薄壳) → 接入 StockDetail 替换现 `buildKlineOption`。深色沿用。

**Tech Stack:** React 18 + TS + Vite · TanStack Query 5 · ECharts 5 · Tailwind · Vitest。

## Global Constraints

- 全部前端代码在 `web/`；不动后端。
- 表现组件纯（props in）；取数只在 hooks/页面；组件测试 mock `../charts/EChart`；option 纯函数单测。
- 深色沿用 v2.1（顺带把 StockDetail 残留浅色类改深色）。红涨绿跌。
- 提交信息结尾加：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 在分支 `v3.2b-frontend` 上开发；每 Task 末尾提交；从 `web/` 跑 vitest/npm。
- 后端契约（v3.2A 已上线）：
  `GET /api/stock/{code}/chart?n=250` → `{code, bars:[{date,open,high,low,close,volume}], ma:{ma5,ma10,ma20,ma60:(number|null)[]}, macd:{dif,dea,hist:(number|null)[]}}`（各序列与 bars 等长）。
- 保留既有 `/kline`+`buildKlineOption`（board 迷你线/其它复用）不动。

---

### Task 1: chart 类型 + client + hook

**Files:**
- Modify: `web/src/api/types.ts`, `web/src/api/client.ts`, `web/src/hooks/queries.ts`
- Test: `web/src/api/client.chart.test.ts`, `web/src/hooks/queries.chart.test.tsx`

**Interfaces:**
- Produces：
  - types：`StockChart{code, bars:{date,open,high,low,close,volume}[], ma:Record<string,(number|null)[]>, macd:Record<string,(number|null)[]>}`
  - client：`getStockChart(code:string, n=250): Promise<StockChart>` → `/stock/${code}/chart?n=${n}`
  - hook：`useStockChart(code:string, n=250)`（`enabled:!!code`，无轮询）

- [ ] **Step 1: 写失败测试**

`web/src/api/client.chart.test.ts`：
```typescript
import { describe, it, expect, vi, afterEach } from "vitest";
import { getStockChart } from "./client";
afterEach(() => vi.restoreAllMocks());

describe("chart client", () => {
  it("getStockChart hits chart path with n", async () => {
    const f = vi.spyOn(globalThis, "fetch").mockResolvedValue({ ok: true, status: 200, json: async () => ({ code: "600000", bars: [], ma: {}, macd: {} }) } as Response);
    await getStockChart("600000", 120);
    expect(f).toHaveBeenCalledWith("/api/stock/600000/chart?n=120");
  });
});
```

`web/src/hooks/queries.chart.test.tsx`：
```tsx
import { describe, it, expect, vi } from "vitest";
import { renderHook, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import React from "react";
vi.mock("../api/client", () => ({
  getStockChart: vi.fn(async () => ({ code: "600000", bars: [{ date: "d", open: 1, high: 2, low: 0.5, close: 1.5, volume: 100 }], ma: { ma5: [null] }, macd: { dif: [0.1], dea: [0.05], hist: [0.1] } })),
}));
import { useStockChart } from "./queries";
function wrapper({ children }: { children: React.ReactNode }) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return <QueryClientProvider client={qc}>{children}</QueryClientProvider>;
}
describe("useStockChart", () => {
  it("returns chart data", async () => {
    const { result } = renderHook(() => useStockChart("600000"), { wrapper });
    await waitFor(() => expect(result.current.isSuccess).toBe(true));
    expect(result.current.data!.bars[0].close).toBe(1.5);
  });
  it("disabled when code empty", () => {
    const { result } = renderHook(() => useStockChart(""), { wrapper });
    expect(result.current.fetchStatus).toBe("idle");
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd web && npx vitest run src/api/client.chart.test.ts src/hooks/queries.chart.test.tsx`
Expected: FAIL

- [ ] **Step 3: 追加 types/client/hook**

`web/src/api/types.ts` 末尾：
```typescript
export interface ChartBar { date: string; open: number; high: number; low: number; close: number; volume: number }
export interface StockChart { code: string; bars: ChartBar[]; ma: Record<string, (number | null)[]>; macd: Record<string, (number | null)[]> }
```

`web/src/api/client.ts`：import 行补 `StockChart`；末尾追加：
```typescript
export const getStockChart = (code: string, n = 250) => apiGet<StockChart>(`/stock/${code}/chart?n=${n}`);
```

`web/src/hooks/queries.ts` 末尾：
```typescript
export const useStockChart = (code: string, n = 250) =>
  useQuery({ queryKey: ["stock-chart", code, n], queryFn: () => api.getStockChart(code, n), enabled: !!code });
```

- [ ] **Step 4: 运行确认通过 + 提交**

Run: `cd web && npx vitest run src/api/client.chart.test.ts src/hooks/queries.chart.test.tsx`
Expected: PASS

```bash
cd /Volumes/demon/code/ml/study/stock
git add web/src/api web/src/hooks
git commit -m "feat(web): 专业K线 client + useStockChart hook

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: buildProKlineOption 三联图 option

**Files:**
- Modify: `web/src/charts/options.ts`
- Test: `web/src/charts/options.pro.test.ts`

**Interfaces:**
- Consumes: `StockChart`
- Produces：`buildProKlineOption(chart: StockChart, plan?: { stop_loss?: number; take_profit?: number }): object`
  - 3 grid（主图/量/MACD）；3 xAxis / 3 yAxis（category 共用 bars 日期）；`axisPointer.link` 三图联动；`dataZoom`(inside+slider) 覆盖 xAxisIndex [0,1,2]
  - series：主图 candlestick(红涨绿跌) + MA5/10/20/60 四 line；量图 bar(涨红跌绿, xAxisIndex/yAxisIndex=1)；MACD dif line + dea line + hist bar(正红负绿, index=2)
  - plan 有值时主图 candlestick 加 markLine(止损/目标)

- [ ] **Step 1: 写失败测试**

`web/src/charts/options.pro.test.ts`：
```typescript
import { describe, it, expect } from "vitest";
import { buildProKlineOption } from "./options";

const chart = {
  code: "600000",
  bars: [
    { date: "d1", open: 10, high: 11, low: 9, close: 10.5, volume: 100 },
    { date: "d2", open: 10.5, high: 12, low: 10, close: 11, volume: 120 },
  ],
  ma: { ma5: [null, 10.7], ma10: [null, null], ma20: [null, null], ma60: [null, null] },
  macd: { dif: [0.1, 0.2], dea: [0.05, 0.1], hist: [0.1, 0.2] },
};

describe("buildProKlineOption", () => {
  it("has 3 grids/xAxis and candlestick+4MA+vol+macd series", () => {
    const opt = buildProKlineOption(chart as any) as any;
    expect(opt.grid).toHaveLength(3);
    expect(opt.xAxis).toHaveLength(3);
    const types = opt.series.map((s: any) => s.type);
    expect(types.filter((t: string) => t === "candlestick")).toHaveLength(1);
    expect(types.filter((t: string) => t === "line")).toHaveLength(6);   // 4 MA + DIF + DEA
    expect(types.filter((t: string) => t === "bar")).toHaveLength(2);    // 量 + MACD hist
    // 蜡烛数据 [open,close,low,high]
    const k = opt.series.find((s: any) => s.type === "candlestick");
    expect(k.data[0]).toEqual([10, 10.5, 9, 11]);
  });
  it("adds markLine when plan given", () => {
    const opt = buildProKlineOption(chart as any, { stop_loss: 9.5, take_profit: 12 }) as any;
    const k = opt.series.find((s: any) => s.type === "candlestick");
    expect(k.markLine).toBeTruthy();
    expect(k.markLine.data).toHaveLength(2);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd web && npx vitest run src/charts/options.pro.test.ts`
Expected: FAIL

- [ ] **Step 3: 追加 options.ts**

`web/src/charts/options.ts` 末尾：
```typescript
import type { StockChart } from "../api/types";

export function buildProKlineOption(chart: StockChart, plan?: { stop_loss?: number; take_profit?: number }): object {
  const dates = chart.bars.map((b) => b.date);
  const kdata = chart.bars.map((b) => [b.open, b.close, b.low, b.high]);
  const vol = chart.bars.map((b) => ({ value: b.volume, itemStyle: { color: b.close >= b.open ? "#ef4444" : "#22c55e" } }));
  const hist = (chart.macd.hist ?? []).map((v) => ({ value: v, itemStyle: { color: (v ?? 0) >= 0 ? "#ef4444" : "#22c55e" } }));
  const maLine = (key: string, color: string) => ({
    name: key.toUpperCase(), type: "line", showSymbol: false, smooth: true,
    data: chart.ma[key] ?? [], lineStyle: { width: 1, color }, xAxisIndex: 0, yAxisIndex: 0,
  });
  const markLine = plan && (plan.stop_loss || plan.take_profit) ? {
    symbol: "none", data: [
      ...(plan.stop_loss ? [{ yAxis: plan.stop_loss, name: "止损", lineStyle: { color: "#22c55e" }, label: { formatter: "止损 {c}" } }] : []),
      ...(plan.take_profit ? [{ yAxis: plan.take_profit, name: "目标", lineStyle: { color: "#ef4444" }, label: { formatter: "目标 {c}" } }] : []),
    ],
  } : undefined;
  return {
    tooltip: { trigger: "axis", axisPointer: { type: "cross" } },
    axisPointer: { link: [{ xAxisIndex: [0, 1, 2] }] },
    legend: { data: ["MA5", "MA10", "MA20", "MA60"], top: 0, textStyle: { color: "#94a3b8" } },
    grid: [
      { left: 50, right: 16, top: 28, height: "52%" },
      { left: 50, right: 16, top: "64%", height: "14%" },
      { left: 50, right: 16, top: "82%", height: "14%" },
    ],
    xAxis: [
      { type: "category", data: dates, gridIndex: 0, axisLabel: { show: false } },
      { type: "category", data: dates, gridIndex: 1, axisLabel: { show: false } },
      { type: "category", data: dates, gridIndex: 2 },
    ],
    yAxis: [
      { scale: true, gridIndex: 0 },
      { scale: true, gridIndex: 1, axisLabel: { show: false } },
      { scale: true, gridIndex: 2, axisLabel: { show: false } },
    ],
    dataZoom: [
      { type: "inside", xAxisIndex: [0, 1, 2], start: 50 },
      { type: "slider", xAxisIndex: [0, 1, 2], bottom: 0, height: 14 },
    ],
    series: [
      { name: "K线", type: "candlestick", data: kdata, xAxisIndex: 0, yAxisIndex: 0,
        itemStyle: { color: "#ef4444", color0: "#22c55e", borderColor: "#ef4444", borderColor0: "#22c55e" },
        ...(markLine ? { markLine } : {}) },
      maLine("ma5", "#f59e0b"), maLine("ma10", "#38bdf8"), maLine("ma20", "#a855f7"), maLine("ma60", "#94a3b8"),
      { name: "量", type: "bar", data: vol, xAxisIndex: 1, yAxisIndex: 1 },
      { name: "DIF", type: "line", showSymbol: false, data: chart.macd.dif ?? [], xAxisIndex: 2, yAxisIndex: 2, lineStyle: { width: 1, color: "#f59e0b" } },
      { name: "DEA", type: "line", showSymbol: false, data: chart.macd.dea ?? [], xAxisIndex: 2, yAxisIndex: 2, lineStyle: { width: 1, color: "#38bdf8" } },
      { name: "MACD", type: "bar", data: hist, xAxisIndex: 2, yAxisIndex: 2 },
    ],
  };
}
```

- [ ] **Step 4: 运行确认通过 + 提交**

Run: `cd web && npx vitest run src/charts/options.pro.test.ts`
Expected: PASS（2 tests）

```bash
cd /Volumes/demon/code/ml/study/stock
git add web/src/charts/options.ts web/src/charts/options.pro.test.ts
git commit -m "feat(web): buildProKlineOption 三联图(K线+MA/量/MACD)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: ProKlineChart 组件 + 接入 StockDetail + 构建冒烟

**Files:**
- Create: `web/src/components/ProKlineChart.tsx`, `web/src/components/ProKlineChart.test.tsx`
- Modify: `web/src/pages/StockDetail.tsx`
- Modify: `README.md`

**Interfaces:**
- Consumes: `useStockChart/useReport`；`buildProKlineOption`；`EChart`
- Produces：
  - `ProKlineChart({ chart, plan? }: { chart: StockChart; plan?: {stop_loss?:number;take_profit?:number} })` —— EChart 薄壳 + buildProKlineOption，height 480
  - `StockDetail` 用 `useStockChart` + `ProKlineChart`（plan 取自 report.decision.battle_plan）替换现 `useKline`+`buildKlineOption` 区；保留 ReportPanel；深色化页面残留浅色类

- [ ] **Step 1: 写失败测试**

`web/src/components/ProKlineChart.test.tsx`：
```tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
vi.mock("../charts/EChart", () => ({ default: () => <div data-testid="echart" /> }));
import ProKlineChart from "./ProKlineChart";

describe("ProKlineChart", () => {
  it("renders chart", () => {
    render(<ProKlineChart chart={{ code: "600000", bars: [], ma: {}, macd: {} }} />);
    expect(screen.getByTestId("echart")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd web && npx vitest run src/components/ProKlineChart.test.tsx`
Expected: FAIL

- [ ] **Step 3: 写组件 + 改 StockDetail**

`web/src/components/ProKlineChart.tsx`：
```tsx
import type { StockChart } from "../api/types";
import EChart from "../charts/EChart";
import { buildProKlineOption } from "../charts/options";

export default function ProKlineChart({ chart, plan }: {
  chart: StockChart; plan?: { stop_loss?: number; take_profit?: number };
}) {
  return <EChart option={buildProKlineOption(chart, plan)} height={480} />;
}
```

`web/src/pages/StockDetail.tsx`（整体替换）：
```tsx
import { useParams, Link } from "react-router-dom";
import { useStockChart, useReport } from "../hooks/queries";
import ProKlineChart from "../components/ProKlineChart";
import ReportPanel from "../components/ReportPanel";

export default function StockDetail() {
  const { code = "" } = useParams();
  const chart = useStockChart(code);
  const report = useReport(code);
  const plan = report.isSuccess ? report.data.decision.battle_plan : undefined;
  return (
    <div className="space-y-4 p-4">
      <Link to="/" className="text-sm text-sky-400">← 返回</Link>
      <section className="rounded-lg border border-slate-700 bg-slate-900 p-4">
        <h2 className="text-lg font-bold text-slate-100">{code} K线</h2>
        {chart.isSuccess ? <ProKlineChart chart={chart.data} plan={plan} />
          : <div className="text-sm text-slate-400">{chart.isError ? "无K线数据" : "加载中…"}</div>}
      </section>
      {report.isSuccess ? <ReportPanel decision={report.data.decision} />
        : <div className="text-sm text-slate-400">{report.isError ? "无研判数据" : "研判加载中…"}</div>}
    </div>
  );
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd web && npx vitest run src/components/ProKlineChart.test.tsx`
Expected: PASS

- [ ] **Step 5: 全套 + 构建**

Run: `cd web && npm test && npm run build`
Expected: 全绿；tsc -b 干净；dist 产出。

- [ ] **Step 6: 端到端冒烟**

```bash
cd /Volumes/demon/code/ml/study/stock
nohup python3 -m server > data_store/api.log 2>&1 &
sleep 6
echo -n "chart: "; curl -s "http://127.0.0.1:8000/api/stock/600519/chart?n=20" | python3 -c "import sys,json;d=json.load(sys.stdin);print(len(d['bars']),'bars, ma60末=',d['ma']['ma60'][-1],'hist末=',d['macd']['hist'][-1])"
cd web && nohup npm run dev > dev.log 2>&1 &
sleep 6
curl -s -o /dev/null -w "个股页 HTTP %{http_code}\n" http://localhost:5173/stock/600519
pkill -f vite; pkill -f "python3 -m server"
```
Expected: chart 返回 bars+MA+MACD；个股页 200。

- [ ] **Step 7: README + 提交**

`README.md` 末尾追加：
```markdown
v3.2 专业K线（/stock/:code）：主图 K线+MA5/10/20/60，副图成交量、MACD，十字光标联动+缩放。
```

```bash
cd /Volumes/demon/code/ml/study/stock
git add web/src/components/ProKlineChart.tsx web/src/components/ProKlineChart.test.tsx web/src/pages/StockDetail.tsx README.md
git commit -m "feat(web): 专业三联图 ProKlineChart 接入个股页

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec 覆盖**（对照 v3.2 第 2 节前端）：
- 三联图(主图K线+MA5/10/20/60 / 副图量 / 副图MACD)+十字光标联动+缩放 → Task 2 buildProKlineOption。✅
- client/hook 取 chart → Task 1。✅
- ProKlineChart 接入 StockDetail、plan 关键价位 markLine → Task 3。✅
- 保留 /kline+buildKlineOption 不动（board 迷你线）。✅
- 深色化 StockDetail 残留浅色类 → Task 3。✅

**占位符扫描**：无 TBD/TODO；每步完整代码。✅

**类型一致性**：`StockChart/ChartBar` 在 types/client/hook/option/组件/页面一致；`useStockChart`、`getStockChart`、`buildProKlineOption` 命名一致；candlestick 数据 [open,close,low,high] 与后端 bars 字段一致；markLine 依赖 plan(battle_plan.stop_loss/take_profit) 字段一致。✅

**范围**：单一可测前端子系统（Vitest 全绿 + build + 冒烟），消费 v3.2A 端点。✅

**已知取舍**：日 K only（分时/周月后续）；MA 只叠主图；MACD hist 正红负绿（与红涨绿跌一致）；StockDetail 之前用的 `buildKlineOption` 保留但该页不再用（board 等其它处仍可用）。
